# Re - re1.exe

## 解题过程

### 1. 样本识别

样本为 `./re1.exe`，PE 可执行文件。

静态分析工具打开后确认文件类型，无加壳特征。

### 2. 入口定位

程序入口点为 PE 标准入口，无特殊跳转逻辑。

主函数调用链简单，未见复杂加密或混淆处理。

### 3. 逻辑还原

使用 `strings` 命令扫描样本字符串区。

```
strings ./re1.exe | grep afctf
```

输出包含完整 flag 字符串：`afctf{w41c0me a1l y0u 9uys}`

该字符串位于数据段，无运行时解密或编码处理。

### 4. 脚本验证

未直接验证程序运行时的 flag 输出行为。

字符串证据已足够，无需进一步动态调试。

## Flag

```
afctf{w41c0me a1l y0u 9uys}
```
