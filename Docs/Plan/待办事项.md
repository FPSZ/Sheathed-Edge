# 藏锋进度与待办事项

更新日期：`2026-03-28`

## 当前状态

- [X] `WSL2 + Ubuntu 24.04` 已完成安装、初始化和验收
- [X] WSL 内 `systemd`、项目挂载、Linux 原生工具链正常
- [ ] Windows 侧 `llama-server` 尚未启动
- [ ] WSL 到 `llama-server` 的健康检查尚未闭环

## 阶段进度

### 当前阶段结论

- [X] 仓库与文档基线已建立
- [X] WSL2 环境基线已建立
- [ ] 模型推理后端尚未接通
- [ ] Windows 侧主模型路径和启动脚本尚未固定

### 已完成阶段

- [X] 仓库骨架已建立
- [X] Git 远程已绑定
- [X] 本地模型系统计划书已完成
- [X] 本地 AI 架构计划已完成
- [X] 后端架构文档已完成并修订
- [X] AI 操作指南和待办体系已建立
- [X] WSL 功能与 runtime 已安装
- [X] `Ubuntu-24.04` 已导入到 `D:\Environment2\WSL\Distros`
- [X] 默认用户已固定为 `awdp`
- [X] WSL 网络模式已切换为 `nat`
- [X] Ubuntu 包源已切到国内可用镜像

### 当前阻塞

- [ ] Windows 侧 `llama-server` 尚未启动
- [ ] 主模型最终文件名和标准目录尚未确认
- [ ] `llama-server` 启动参数、启动脚本、健康检查尚未固定

### 下一步

- [ ] 导出 WSL baseline 快照到 `D:\Environment2\WSL\Export\ubuntu-24.04-baseline.tar`
- [ ] 整理主模型文件名和目录，固定 `gguf` 标准路径
- [ ] 编写 Windows 侧 `llama-server` 启动脚本
- [ ] 启动 `llama-server`，完成 `127.0.0.1:8080` 健康检查
- [ ] 再次执行 `validate-wsl.ps1`，闭环模型服务可达性

## 后续阶段

### 自动练题学习闭环

- [ ] 设计本地靶场自动练题调度器
- [ ] 设计题目领取、超时、重试、收题规则
- [ ] 设计自动练题的模式组合限制：`awdp` / `awdp+web` / `awdp+pwn`
- [ ] 设计解题结果判定器，确保不是“自认为做对”而是靶场验收通过
- [ ] 设计成功样本入主知识库规则
- [ ] 设计失败样本入失败库规则
- [ ] 设计题目、exp、patch、误判点、工具调用链的统一回灌格式
- [ ] 设计自动总结经验的模板，区分“正确经验”和“错误经验”
- [ ] 设计人工审查层，避免知识库被错误样本污染
- [ ] 评估是否将高质量样本转入后续训练 / 微调数据集

## 主线任务

### 仓库与文档基线

- [X] 项目目录骨架已创建
- [X] Git 仓库已初始化并绑定远程
- [X] 本地模型系统计划书已完成
- [X] 本地 AI 架构计划已完成
- [X] 后端架构文档已建立并修订
- [X] AI 操作指南完成并纳入执行规范
- [X] 待办事项文档持续维护机制固定

### 模型与推理后端

- [ ] 模型下载完成并验证文件名
- [ ] 模型路径固定到标准目录
- [ ] 验证 `llama-server` 从 Windows 可正常启动
- [ ] 验证 WSL2 可以访问 `llama-server`
- [ ] 固定 `llama-server` 启动参数基线
- [ ] 验证 `32768` 上下文基线是否稳定

### Gateway

- [ ] 定义 Gateway 工程目录与模块边界
- [ ] 定义 `GET /healthz`
- [ ] 定义 `GET /v1/models`
- [ ] 定义 `POST /v1/chat/completions`
- [ ] 定义 OpenAI-compatible SSE 输出策略
- [ ] 定义 `tool_call` 状态机
- [ ] 定义 fail-closed + repair retry 策略
- [ ] 明确 Gateway 内置检索流程

### Tool Router

- [ ] 定义 Tool Router 工程目录与模块边界
- [ ] 定义 `POST /internal/tools/resolve`
- [ ] 定义 `POST /internal/tools/execute`
- [ ] 定义工具错误响应格式
- [ ] 定义工具审计日志格式
- [ ] 明确 registry 加载和 scope 判定逻辑

### MCP Adapters

- [ ] 定义 `filesystem/search` adapter
- [ ] 定义 `retrieval` adapter
- [ ] 定义 `browser` adapter
- [ ] 定义 `radare2` adapter
- [ ] 定义 `terminal:web` adapter
- [ ] 定义 `terminal:pwn` adapter

### 检索与知识库

- [ ] 明确 Gateway 内置检索与 retrieval tool 的职责边界
- [ ] 设计 `SQLite FTS5` 索引结构
- [ ] 固定 `ripgrep` 兜底规则
- [ ] 整理 `Core/AWDP` 知识源
- [ ] 整理 `Plugins/web/datasets`
- [ ] 整理 `Plugins/pwn/datasets`

### Open WebUI 接入

- [ ] 让 Open WebUI 只连接 Gateway
- [ ] 验证 `model` 列表兼容性
- [ ] 验证流式 SSE 兼容性
- [ ] 完成 Open WebUI smoke test

### 日志与评测

- [ ] 固定会话日志格式
- [ ] 固定工具日志格式
- [ ] 固定评测日志格式
- [ ] 建首批 `awdp` 评测 case
- [ ] 建首批 `awdp+web` 评测 case
- [ ] 建首批 `awdp+pwn` 评测 case
- [ ] 建首批复合题评测 case

## 支线任务

### 命名与 README

- [X] 仓库已绑定远程仓库
- [X] 确认项目中文名与英文名
- [ ] 重写 README 为项目简介和架构总览
- [ ] 补许可证说明

### `.gitignore` 与仓库卫生

- [X] `.gitignore` 已覆盖模型、日志、索引、构建产物
- [ ] 确认是否保留 `Cargo.lock`
- [ ] 确认 `.vscode` 是否长期忽略
- [ ] 检查是否有不该提交的本机文件

### WSL2 / 环境准备

- [X] 固定 WSL2 安装目录为 `D:\Environment2\WSL`
- [X] 生成 WSL 安装、初始化、验收脚本
- [X] 下载 Ubuntu 24.04 官方 rootfs 并完成 `SHA256` 校验
- [X] WSL2 Ubuntu 环境就绪
- [X] 以管理员身份启用 WSL 和 VirtualMachinePlatform
- [X] 导入 `Ubuntu-24.04` 到 `D:\Environment2\WSL\Distros`
- [X] 执行 `bootstrap-ubuntu.sh` 完成 Linux 基线
- [X] 安装 `go`
- [X] 安装 `rustup + stable`
- [X] 安装 `uv`
- [X] 安装 `python3`
- [X] 安装 `sqlite3`
- [X] 安装 `ripgrep`

### 靶场与测试样例

- [ ] 准备最小 web 靶场
- [ ] 准备最小 pwn 样例
- [ ] 准备本地 browser 白名单验证目标
- [ ] 准备 radare2 分析样例

### 数据整理与训练集

- [ ] 建 `train/dev/eval` 目录规范
- [ ] 导入第一批 web 数据
- [ ] 导入第一批 pwn 数据
- [ ] 记录失败 case 的回灌格式
- [ ] 预留自动练题样本的入库目录和命名规范

### web 插件细化

- [ ] 细化 `browser` 动态目标集策略
- [ ] 定义 `terminal:web` 命令模板白名单
- [ ] 细化 web prompt 片段
- [ ] 细化 web eval 标签

### pwn 插件细化

- [ ] 定义 `terminal:pwn` 命令模板白名单
- [ ] 细化 radare2 使用边界
- [ ] 细化 pwn prompt 片段
- [ ] 细化 pwn eval 标签

## 运营与纪律

- [ ] 每次工作会话结束后写入 `Docs\Log\2026-03-27.txt`
- [ ] 每完成一个阶段后更新本待办
- [ ] 每发现一个架构风险后补日志并标记阻塞
- [ ] 每周回看一次文档一致性
- [ ] 定期检查计划文档是否与实现漂移
