# 藏锋当前进度与待办事项

更新日期：`2026-03-29`

## 当前状态

- [x] `WSL2 + Ubuntu 24.04` 已完成安装、初始化和验收
- [x] `Go / Rust / Python / sqlite3 / ripgrep / uv` 基线工具链已可用
- [x] `Gateway` 最小骨架已实现并通过本地编译
- [x] `Tool Router` 最小骨架已实现并通过本地编译
- [x] `Gateway` 已按 `provider / mode / retrieval / envelope / orchestrator / transport / logging` 完成模块化拆分
- [x] `Tool Router` 已按 `api / config / registry / policy / executor / logging` 完成模块化拆分
- [x] `Gateway /healthz` 与 `/v1/models` 已完成本地验证
- [x] `Tool Router /healthz`、`resolve`、`execute` 已完成本地验证
- [x] Open WebUI 已在 WSL 安装完成
- [x] Windows 侧 `llama-server` 已安装、启动并通过 `/health`、`/v1/models`、`/v1/chat/completions` 验证
- [x] `Gateway -> llama-server` 无工具推理链路已闭环
- [x] Open WebUI 已在 WSL 以离线模式启动并通过 `/health` 验证
- [ ] `Gateway` 单轮 `tool_call -> Tool Router -> 二次推理 -> 最终答复` 仍未完整闭环

## 阶段结论

- [x] 下一阶段主线已经从“纯文档设计”进入“最小可运行骨架”
- [x] 插件扩展面的核心字段已冻结：`prompt_files`、`skill_files`、`tool_scope`、`retrieval_roots`、`eval_tags`、`plugin_capabilities`
- [x] 工具注册表已预留 `tools / resources / prompts` 三类能力位
- [x] `action-envelope` 已补齐 `answer` 正文承载字段 `content`
- [x] Gateway 已补上 `127.0.0.1 -> host IP fallback`
- [x] WSL 构建缓存策略已收紧，避免 Go/Rust 缓存权限漂移
- [x] 下一阶段要求的“禁止继续堆单文件”已落地到 Gateway 与 Tool Router 代码结构

## 当前阻塞

- [ ] `Gateway` 的单轮 `tool_call` 路径仍存在服务端异常：模型已产出 `tool_call`、`Tool Router` 已执行成功，但客户端收到空回复，最终答复写回仍需修复
- [ ] Open WebUI 虽已离线启动，但尚未完成 UI 侧 provider 配置与聊天 smoke test

## 主线任务

### 1. 推理后端
- [x] 固定 `Agent/llama-server.config.json`
- [x] 固定 Windows 启动脚本：`Workflows/windows/start-llama-server.ps1`
- [x] 固定健康检查脚本：`Workflows/windows/check-llama-server.ps1`
- [x] 确认 `llama-server.exe` 实际路径
- [x] 确认主模型 `DeepSeek-R1-Distill-Llama-70B-Q4_K_M.gguf` 实际路径
- [x] 启动 `llama-server`
- [x] 验证 `http://127.0.0.1:8080/health`
- [x] 验证 `http://127.0.0.1:8080/v1/models`
- [x] 验证 `http://127.0.0.1:8080/v1/chat/completions`
- [x] 从 WSL 验证宿主机 IP fallback 可访问 `llama-server`
- [ ] 若继续保持 `WSL NAT`，补充 Windows 防火墙/监听面约束说明，固定 `0.0.0.0:8080` 的安全边界

### 2. Gateway
- [x] 建立 `provider / mode loader / retrieval / tool client / server` 分层
- [x] 实现 `GET /healthz`
- [x] 实现 `GET /v1/models`
- [x] 实现 `POST /v1/chat/completions` 最小骨架
- [x] 实现 `answer` / `tool_call` action envelope 解析
- [x] 实现 malformed JSON repair retry 一次
- [x] 实现工具不可用时的 fail-closed fallback
- [x] 实现会话日志落盘
- [x] 用真实 `llama-server` 验证无工具回答路径
- [x] 修复 `127.0.0.1 -> host IP fallback` 的端口丢失 bug
- [x] 修复代码块包裹的 action envelope 解析
- [ ] 用真实 `llama-server` 验证 `answer` envelope -> 最终正文解包
- [ ] 修复 `tool_call` 路径的空回复问题，完成 `Tool Router -> 二次推理 -> 最终答复`
- [ ] 补 `N` 轮工具调用状态机与熔断
- [ ] 调整 SSE 事件切片，做 Open WebUI 兼容性回归

### 3. Tool Router
- [x] 建立 Rust 最小服务骨架
- [x] 实现 `POST /internal/tools/resolve`
- [x] 实现 `POST /internal/tools/execute`
- [x] 实现参数 schema 校验
- [x] 实现路径归一化与目录约束
- [x] 实现工具日志落盘
- [x] 实现 `filesystem/search`
- [x] 实现 `retrieval`
- [x] 实现 `terminal` 第一版受控通用 Shell 执行器（PowerShell 优先，可选 WSL bash）
- [x] 为 `browser / radare2 / terminal:web / terminal:pwn` 预留注册表与返回契约
- [ ] 为 `browser` 增加静态白名单 + session 动态目标集双重校验
- [ ] 为 `terminal:web` 明确模板白名单和参数绑定
- [ ] 为 `terminal:pwn` 明确模板白名单和参数绑定
- [ ] 把 `not implemented` 返回码写入统一错误码表文档

### 4. 模式与插件
- [x] `awdp` 主模式配置已固定
- [x] `web` 插件配置已固定
- [x] `pwn` 插件配置已固定
- [x] `plugin_capabilities` 字段已加入核心配置
- [ ] 增加一个占位新插件样例，验证不改 Gateway 主流程即可接入
- [ ] 细化 `web` prompt 片段与数据集组织
- [ ] 细化 `pwn` prompt 片段与数据集组织

### 5. Open WebUI 接入
- [x] 在 WSL 安装 Open WebUI
- [x] 以离线模式启动 Open WebUI
- [ ] 只接入 Gateway 作为唯一 provider
- [ ] 验证 `/v1/models` 可被识别
- [ ] 验证普通聊天
- [ ] 验证流式 SSE
- [ ] 验证 WebUI 不感知底层 GGUF 与 MCP 细节

### 6. 检索与知识库
- [x] 区分 Gateway 内置检索与 `retrieval` tool 职责
- [x] 内置检索先用 `ripgrep` 兜底
- [ ] 建立 `SQLite FTS5` 索引脚本和 schema
- [ ] 整理 `Core/awdp`
- [ ] 整理 `Plugins/web/datasets`
- [ ] 整理 `Plugins/pwn/datasets`
- [ ] 清理不存在的 `Writeups` 路径引用，或补回目录基线

## 支线任务

### 仓库与文档
- [x] Git 仓库已初始化并绑定远程
- [x] `.gitignore` 已建立
- [x] `本地模型系统计划书.md` 已完成
- [x] `本地AI架构计划.md` 已完成
- [x] `后端架构.md` 已完成并修订
- [x] `AI操作指南.md` 已完成
- [ ] 重写顶层 `README.md` 为开源说明和快速启动

### WSL 与运行维护
- [x] WSL 缓存目录策略已固定到 Linux 用户目录
- [x] WSL 环境变量已补 `GOCACHE`
- [x] Open WebUI 虚拟环境已落在 WSL 用户目录
- [x] Open WebUI 已切换为离线启动参数集
- [ ] 重新执行 `validate-wsl.sh`，确认新环境变量全通过
- [ ] 导出 `ubuntu-24.04-baseline.tar` 快照

### 自动练题闭环预留
- [x] 已预留 `eval_tags`
- [ ] 设计 `success_cases / failure_cases / review_queue` 目录规范
- [ ] 设计自动练题结果判定器
- [ ] 设计经验回灌模板
- [ ] 设计人工审核门，防止错误样本污染知识库

## 纪律

- [ ] 每次工作会话结束后写入 `Docs/Log/2026-03-28.txt`
- [ ] 每完成一个阶段后更新本文件
- [ ] 每发现一个结构性风险后追加日志和阻塞项

## 2026-03-28 最新进展

- [x] `Gateway` 已补 `panic recovery`、响应校验与结构化 `provider_error`，`/v1/chat/completions` 不再出现空连接关闭
- [x] `Gateway` 已新增按 `request_id` 记录的阶段日志：`provider_first / envelope_parse / tool_resolve / tool_execute / provider_second / final_writeback`
- [x] `Tool Router` 已补 `retrieval` 参数归一化，`max_docs -> limit` 不再导致 `resolve` 失败
- [x] 固定回放 `tmp-gateway-toolcall.json` 已验证单轮 `tool_call -> Tool Router -> provider_second -> final_writeback` 闭环
- [x] Open WebUI 已通过 `Gateway` 识别唯一模型 `awdp-r1-70b`
- [x] Open WebUI 已完成最小 provider smoke test：`/openai/models` 与 `/openai/chat/completions` 可用
- [x] `Gateway` 直连与 `Open WebUI -> Gateway` 代理两条 `stream=true` 路径均已通过 SSE 回归
- [x] `awdp` prompt 已从“默认结构化约束”改为“身份 + 能力 + 输出模式”，普通聊天与任务请求已能分流
- [x] Open WebUI 后台 `follow-up/title/tags/query/autocomplete` 生成已关闭，避免一次聊天后追加模型请求
- [x] `Gateway` 已在普通对话路径透传 `reasoning_content`、`usage`、`timings`
- [ ] 二次推理答案质量仍需继续收敛，当前链路已通但输出仍有波动
- [ ] 任务型请求与 `tool_call` 路径的真实流式透传仍待扩展
## Gateway Admin GUI V1
- [x] 在 `Gateway` 新增 `/internal/admin/*` 路由组与 `/admin` 静态托管入口
- [x] 新增 `internal/gateway/admin` 服务层，聚合 services / models / modes / session logs / tool logs
- [x] 新增 `Agent/model-profiles.json`，预置 `deepseek-r1-70b-q4-competition` 与 `qwen3.5-35b-a3b-q8-experimental`
- [x] 已将 `qwen3.5-35b-a3b-q8-experimental` 设为可用，Admin GUI 模型页可展示为可切换状态
- [x] 新增 `ui/gateway-admin` 前端工程，完成 Dashboard / Models / Modes / Logs / Settings 骨架
- [x] 验证 `ui/gateway-admin` 执行 `npm run build` 通过
- [x] 验证 `Agent/gateway-go` 执行 `go build ./...` 与 `go test ./...` 通过
- [ ] 补齐 Windows Build Tools / Windows SDK，使 `Agent/host-control-rs` 可完成本机构建
- [ ] 启动 `host-control-rs`，验证 `GET /healthz` 与 `GET /internal/host/llama/status`
- [ ] 联调 `POST /internal/admin/models/switch` 与 `POST /internal/admin/llama/start|stop|restart`
- [ ] 用浏览器验证 `/admin` 页面可展示实时状态并执行模型启停/切换

## 当前最新进度
- [x] `llama-server` 已手动关闭，`8080` 监听已释放
- [x] Admin GUI 第一阶段代码已落地：`Gateway Admin API`、`ui/gateway-admin`、`Agent/host-control-rs` 工程骨架均已建立
- [x] `ui/gateway-admin` 已通过 `npm run build`
- [x] `Agent/gateway-go` 已通过 `go build ./...` 与 `go test ./...`
- [ ] `Agent/host-control-rs` 仍受 Windows SDK / MSVC 链接库缺失阻塞，当前无法完成本机构建
- [ ] 后续运行环境、工具链、缓存和宿主机侧可执行文件继续统一收口到 `D:\Environment2`

## 2026-03-29 最新进度
- [x] `Core/awdp/prompts/system.md` 已切换为“授权比赛态”提示词，明确 AWDP/CTF 场景下的预授权边界，减少法律/伦理误拒绝
- [x] `Gateway /v1/models` 已改为基于 `Agent/model-profiles.json` 暴露全部启用模型，准备给 Open WebUI 提供双模型下拉选择
- [x] `Gateway /v1/chat/completions` 已支持按请求里的 `model` 选择目标 profile，并在需要时走 `Host Agent` 执行模型切换
- [x] `orchestrator` 已从单一全局 `model alias` 改为按请求传入响应模型，避免返回值继续写死旧模型名
- [x] `ui/gateway-admin/src/pages/ModelsPage.tsx` 已重写，可区分当前活动模型、运行状态，并提供“设为活动模型”“切换并启动”“停止此模型”等操作
- [x] `Tool Router` 已为 `terminal` 落地第一版受控通用 Shell 执行器，默认 `PowerShell`，按参数可切换到 `WSL bash`
- [x] `terminal` 第一版访问边界已固定为工作区与 `D:\Environment2`，支持 `command / workdir / timeout_ms / shell` 结构化入参
- [x] `Agent/tool-router-rs` 已通过 `cargo test` 与 `cargo build`
- [x] `terminal` 已在临时 `tool-router-rs` 实例上完成 `resolve`、`PowerShell`、`WSL bash`、超时与非法路径拒绝验证
- [x] `Agent/gateway-go` 已再次通过 `go build ./...` 与 `go test ./...`
- [x] `ui/gateway-admin` 已再次通过 `npm run build`
- [x] `Gateway` 的 terminal 代码块自动执行 fallback 已移除，不再根据普通文本或 fenced code block 猜测执行本机命令
- [x] `Agent/tool-router-rs` 已新增 `GET /openapi.json` 与 `POST /api/tools/terminal`，可作为 Open WebUI External Tools 的 OpenAPI Tool Server
- [x] `ui/gateway-admin / Settings` 已新增 Tool Server 信息区，显示 Tool Router 地址、OpenAPI spec 与 terminal endpoint
- [x] `Gateway -> llama-server` 已补上原生 OpenAI tool calling 透传，支持 `tools/tool_choice/tool_calls/tool_call_id`
- [x] `Gateway` 已新增 native tool path 分流：带原生 tools 的请求不再进入 legacy envelope 编排
- [x] 已验证 `Gateway` 的 native tool calling：首轮返回 `tool_calls`、二轮 tool result 回传后返回最终答复、流式模式输出标准 SSE `tool_calls`
- [x] Open WebUI 模型 `qwen3.5-35b-a3b-q8` 已写入 `function_calling=native`
- [ ] 在 Open WebUI `External Tools` 中接入 `tool-router-rs /openapi.json`，完成 terminal 调用闭环验证
- [ ] `Agent/host-control-rs` 仍因宿主机缺少 Windows SDK / MSVC 系统库而无法完成构建，真实模型切换联调仍被阻塞
- [ ] 待 `host-control-rs` 可运行后，去 Open WebUI 验证两个模型同时可见且选中后会触发底层模型切换

## 2026-03-30 最新进展
- [x] `SSH Remote Terminal V1` 已接入 `Agent/tool-router-rs`，`terminal` 现支持 `local + ssh` 双后端，覆盖 `transport / host_id / remote_shell / workdir / timeout_ms` 结构化参数
- [x] SSH 执行链已支持密码认证、私钥认证、首次 host key 确认、后续指纹严格校验、远程 `workdir` 白名单限制与结构化错误返回
- [x] `tool-router-rs` 已补齐 SSH 相关接口与 OpenAPI 终端服务联动，当前可通过 `GET /openapi.json`、`POST /api/tools/terminal`、`POST /internal/ssh/test` 走通本地与 SSH terminal 能力
- [x] `Gateway Admin API` 已新增 SSH 管理接口：`/internal/admin/ssh/hosts`、`/internal/admin/ssh/hosts/test`、`/internal/admin/ssh/hosts/confirm-host-key`、`/internal/admin/ssh/bindings`
- [x] `ui/gateway-admin` 的 `Settings` 已接入 SSH Hosts 与用户默认主机绑定配置，可读取 `Agent/ssh-hosts.json` 与 `Agent/ssh-user-bindings.json`
- [x] `Workflows/wsl/patch-open-webui-default-terminal.py` 已补上 `transport=ssh`、`host_id`、`user_email` 默认注入逻辑，保持 Open WebUI 官方 External Tools 路径，不再回退到代码块自动执行
- [x] Admin UI 右侧栏已重做为“统一骨架 + 页面专属模块”的 utility rail，已补上日志选中预览、页面摘要、右栏溢出修复与滚动条隐藏
- [x] 构建验证已完成：`Agent/tool-router-rs` 通过 `cargo build`，`Agent/gateway-go` 通过 `go build ./...`，`ui/gateway-admin` 通过 `npm run build`
- [ ] `ssh-hosts.json` 与 `ssh-user-bindings.json` 仍缺真实主机资料，尚未完成“真实队友机器 / 真实 SSH 主机”端到端联调
- [ ] Admin UI 主内容区视觉仍比新版右栏更重，后续可继续统一阴影、留白与信息密度
- [x] `Admin UI` 已完成“用户工作区化”第一阶段：新增 `AdminScopeContext`、全局用户选择器、`user-settings.json`、`/internal/admin/users` 与 `/internal/admin/users/workspace`，`Logs / Settings / Dashboard` 已能按 `Open WebUI` 邮箱切换视角
- [x] `Gateway` 与 `Tool Router` 日志链路已补 `user_email`，Open WebUI 补丁会向聊天请求与 terminal 工具请求统一注入用户邮箱，当前日志筛选已具备按用户聚合的基础
- [x] 已新增一键启动脚本 `Workflows/windows/start-admin-ui.ps1`，可直接拉起 `host-agent / tool-router / gateway / open-webui` 并打开 `/admin`
- [x] `Admin` 服务控制已修复 `tool-router` 启动链路：优先使用已编译的 `tool-router-rs.exe`，避免离线环境下 `cargo run` 拉索引失败
- [x] `Admin` 服务控制已实测 `tool-router` 与 `open-webui` 的 `stop -> start` 闭环，Dashboard 按钮也已按实时状态禁用无效操作
- [ ] `host-agent` 仍存在 Windows 端口残留问题：强制停止后 `8101` 可能出现死监听，导致后续健康检查失败；当前已暂时禁用 `host-agent stop`
- [ ] 下一步优先补 `host-agent` 的优雅关闭与端口释放逻辑，恢复 `start/stop` 完整可控性
