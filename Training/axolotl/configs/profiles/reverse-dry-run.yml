# Reverse dry-run profile.
# Intended for preprocessing and a minimal 1-2 step smoke test.

profile_id: "reverse-dry-run"
max_steps: 2
num_epochs: 1
save_steps: 2
logging_steps: 1
warmup_steps: 0
evals_per_epoch: 0
