# Teacher Notes

- Alexander17 的复盘把主洞定位在 `MULTI` 事务实现：重复 `SET` 绕过引用计数直接 free 旧 value、tx chunk 未初始化且释放后仍被全局指针继续使用。
- S1nyer 复盘与本地字符串都确认了命令集 `SET/GET/CLONE/MULTI/EXEC/ABORT`，说明至少这套题面接口是一致的。
- 旧的 CN-SEC 文章只能做弱对照，不能直接当标准答案；它的价值在于帮助理解对象布局与历史题型，不应越过本地核对。
- 训练中应把这题标为『事务/状态机型二进制服务』，优先要求模型恢复对象关系、引用计数和事务生命周期。
