# Red Cases

这里放已经人工确认、适合复盘和后续训练 review 的红队案例。

建议每个 case 子目录至少包含：
- `meta.json`
- `challenge.md` 或 `service.md`
- `teacher-notes.md`
- `artifacts/`（可选）
- `wp.md`（若允许沉淀）

不要把未核对、不可复现、来源不明的 exploit 样本直接丢进这里。
