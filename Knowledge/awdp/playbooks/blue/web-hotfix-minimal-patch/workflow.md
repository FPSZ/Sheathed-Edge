# Workflow

## Intake
- 先确认漏洞入口、受影响参数、可复现 payload、业务依赖点
- 标记 checker 可能依赖的 URL、字段、状态码、页面关键字

## Patch
- 优先选择最小热修：输入校验、路径白名单、模板上下文隔离、命令参数固定化、关闭危险分支
- 改动尽量收在入口层，不先大改底层结构
- 若不得不变更行为，先找兼容层或兜底返回

## Verify
- 用原 exploit 验证修复是否生效
- 再验证关键业务路径是否仍正常
- 记录 diff、回滚点、影响面

