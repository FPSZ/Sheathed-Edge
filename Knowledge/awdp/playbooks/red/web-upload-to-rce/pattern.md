# Pattern

适用场景：
- Web 服务存在上传点、导入点、附件点、头像点、插件包导入点
- 目标可控项包括文件名、扩展名、Content-Type、保存路径、二次访问路径
- 最终目标是把“能上传”推进到“能执行”或“能包含”

核心判断：
- 先区分这是单纯文件落盘，还是可直接访问执行的上传链
- 先收缩到最短可复现上传样本，再逐步变体扩展名、解析链、路径穿越、双后缀
- 一旦形成可执行链，马上转到 harvest / reuse，不继续无节制 fuzz

