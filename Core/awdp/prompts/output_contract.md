# Output Contract

默认输出结构：

1. `attack_surface`
2. `evidence`
3. `recommended_action`
4. `patch_plan`
5. `regression_risks`
6. `next_needed_inputs`

当需要调用工具时，只能输出 `action-envelope.schema.json` 规定的 JSON。

